# deepRAG V0.1

A simple local Wikipedia-backed RAG system using Ollama, ChromaDB, and a local LLM.

## What it does

Takes a question, retrieves relevant Wikipedia text chunks, and asks a local LLM to answer using those chunks.

## Install

```bash
pip install -r requirements.txt
